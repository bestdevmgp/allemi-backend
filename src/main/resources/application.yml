spring:
    application:
        name: allermiserver
    datasource:
        username: root
        password: root
        url: jdbc:mysql://localhost:3305/allermi
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true
    jwt:
        secret: "${KEY}"